# 快速修复说明

## 修复的问题

### 问题1：创意只有2个
**原因**：AI可能只生成2个创意，或解析失败
**修复**：
- 当创意不足5个时，自动用默认创意补齐
- 添加了5个高质量默认创意（符合玛丽苏套路）

### 问题2：主角空缺
**原因**：角色解析失败，主角name为空
**修复**：
- 添加文本预处理（移除特殊字符）
- 如果主角无效，直接返回完整默认角色列表
- 添加详细日志追踪解析过程

## 立即测试

### 方法1：正常测试
1. 刷新浏览器（Ctrl+Shift+R）
2. 测试创意生成 → 应该显示5个
3. 测试角色生成 → 应该6个都有名字

### 方法2：一键修复（如果方法1失败）
打开控制台（F12），运行：
```javascript
DebugHelper.fixAll()
```

这会立即用默认数据替换所有缺失/无效的数据。

## 应急命令

```javascript
// 仅修复创意
DebugHelper.fixIdeas()

// 仅修复角色
DebugHelper.fixCharacters()

// 检查当前状态
DebugHelper.checkProject()
```

## 如果还有问题

请打开控制台，复制以下日志给我：
- `[AI服务]` 开头的日志
- `[角色解析]` 开头的日志
- `[创意解析]` 开头的日志
